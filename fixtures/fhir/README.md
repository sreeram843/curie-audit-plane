# Synthetic fixtures (non-PHI)

All files in this directory are fully synthetic. Names (`Jane Test`), identifiers (`TEST-00001`),
and dates are fictional. They contain no real protected health information and are safe to commit.
